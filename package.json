{
  "name": "messenger-root",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm run build",
    "install": "cd backend && npm install"
  },
  "private": true
}