# Railway Configuration File
# Docs: https://docs.railway.app/deploy/configuration-file

[build]
builder = "nixpacks"

[nixpacks]
phases = { setup = { nixPkgs = ["nodejs", "npm"] }, install = { cmd = "cd backend && npm install" }, build = { cmd = "cd backend && npm run build" } }
startCommand = "cd backend && npm start"